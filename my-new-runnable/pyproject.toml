[tool.poetry]
name = "my-new-runnable"
version = "0.1.0"
description = ""
authors = ["y>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
cyclarity-sdk = "^1.0.55"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
